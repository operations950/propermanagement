Django==6.0.7
python-dotenv==1.2.2
APScheduler==3.11.3
python-dateutil==2.9.0.post0
requests==2.34.2
anthropic==0.69.0
google-auth-oauthlib==1.2.2
google-api-python-client==2.184.0

# Production deployment (Railway) — see settings.py's DATABASES/MIDDLEWARE
# and the Procfile. Not used by local dev (SQLite + runserver).
gunicorn==26.0.0
whitenoise==6.12.0
dj-database-url==3.1.2
psycopg[binary]==3.3.4
